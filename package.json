{
  "name": "currency-converter",
  "version": "1.0.0",
  "description": "",
  "main": "bin/currency-course-provider.js",
  "scripts": {
    "ts:compile": "./node_modules/.bin/tsc",
    "build": "npm run ts:compile",
    "test-server": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "request": "^2.88.0"
  },
  "devDependencies": {
    "typescript": "^3.3.4000"
  }
}
